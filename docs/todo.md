# TODO

全面リファクタ（Phase 1〜5）はコミット済みで完了。以下は残タスク。重要度が高い順に記載し、Claude Codeでの想定工数（小/中/大）を併記する。

## 1. 「おすすめ」タブの初回表示不具合の調査・修正

- 優先度: 高
- 目安工数: 中〜大（原因未特定のため調査次第で変動）
- 症状: 他のタブから「おすすめ」タブへ移動すると初回は表示されず、再度タブをタップすると正常に表示される。
- 状況: `ng serve`（開発サーバー）で再現。ブラウザコンソールに赤字のエラーは出ていない。
- 備考: ユーザー向けの実害があるバグのため最優先で着手する。

## 2. `restaurant-sync.service.spec.ts` の追加

- 優先度: 高
- 目安工数: 小〜中
- 内容: Firestore自動同期のdiff-basedスナップショット方式（`diffChanged()`）の回帰テスト。
  - 無変更時にpush対象0件になること
  - 1件更新時にその1件のみpush対象になること
  - ログインユーザー切替時にスナップショットがリセットされること
- 備考: 直近実装した新しいロジックのため最優先でテストを整備する。

## 3. `recommendation-scorer.spec.ts` の追加

- 優先度: 中
- 目安工数: 小
- 内容: ベイズ平均評点、Haversine距離、直近ピック減点、`precomputedDistance`引数を渡した場合の回帰テスト。

## 4. `opening-hours.spec.ts` の追加

- 優先度: 中
- 目安工数: 小
- 内容: `getRemainingOpenMinutes()`の日跨ぎ境界値（深夜営業パターン）のテスト。

## 5. `restaurant-store.spec.ts` の追加

- 優先度: 中
- 目安工数: 小〜中
- 内容: `addMany`の重複排除、update/remove（tombstoneによる論理削除）、`replaceAll`のテスト。

## 6. `csv-import.spec.ts` の追加

- 優先度: 低
- 目安工数: 小
- 内容: Google Takeout CSVの代表パターン（欠損フィールド、複数ファイルの結合）のテスト。
